export const runtime = "nodejs";
export const dynamic = "force-dynamic";
export const revalidate = 0;

// Re-export ONLY handlers (Next.js cannot statically parse re-exported config fields)
export { GET, POST } from "../../moves/route";
